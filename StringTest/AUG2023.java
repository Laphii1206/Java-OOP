package StringTest;

import java.util.*;

public class AUG2023 {
    public static void main(String[] args) {
        char letter = keyboard.charAt(2);

        String half = message.substring(0, message.length() / 2);
        
        if (s1.compareTo(s2) > 0)

        String firstLast = "" + message.charAt(0) + message.charAt(message.length() - 1);
    }
}